# TechShop

## Практика 7 (JWT-аутентификация)
- Реализована регистрация и вход пользователя с выдачей `accessToken` и `refreshToken`.
- Добавлен защищенный маршрут получения текущего пользователя `GET /api/auth/me`.
- На фронтенде настроено сохранение токенов и автоматическая авторизация после входа.

## Практика 8 (Refresh Token)
- Реализовано обновление пары токенов через маршрут `POST /api/auth/refresh`.
- На клиенте добавлен механизм автоматического обновления access-токена при ответе `401`.
- Сессия пользователя сохраняется между перезагрузками страницы.

## Практика 9 (Защищенные маршруты и API)
- Backend и frontend объединены в одно приложение интернет-магазина `Tech Shop`.
- Реализованы маршруты для работы с товарами: создание, получение списка, получение по `id`, обновление и удаление.
- Добавлены middleware для логирования запросов, обработки `404` и серверных ошибок.

## Практика 10 (Авторизация и интерфейс)
- Создан интерфейс входа и регистрации на React.
- Пользователь после входа получает доступ к рабочему интерфейсу приложения.
- Реализован удобный frontend для просмотра и управления товарами через REST API.

## Практика 11 (RBAC)
- Добавлена система ролей: `Гость`, `Пользователь`, `Продавец`, `Администратор`.
- Пользователь может только просматривать товары.
- Продавец может создавать и редактировать товары.
- Администратор может управлять товарами и пользователями.
- Реализованы маршруты `/api/users` для просмотра, редактирования и блокировки пользователей.
- Добавлен `roleMiddleware` для ограничения доступа к маршрутам по ролям.

## Практика 13 (Service Worker)
- Зарегистрирован Service Worker (`frontend/public/sw.js`).
- App Shell кэшируется при первой установке.
- При отсутствии сети приложение загружается из кэша (офлайн-режим).

## Практика 14 (Web App Manifest)
- Добавлен `manifest.json` с полями `name`, `short_name`, `display: standalone`, `theme_color`, `icons`.
- Сгенерированы PNG-иконки 8 размеров (16–512px).
- Добавлены мета-теги для iOS и Android.
- Приложение можно установить на устройство как PWA.

## Практика 15 (HTTPS + App Shell)
- React SPA по природе является App Shell-архитектурой.
- Service Worker реализует стратегию **Cache First** для иконок/манифеста и **Network First** для страниц.
- Для запуска по HTTPS локально используется `mkcert` + `http-server --ssl`.

## Практика 16 (WebSocket + Push)
- Бэкенд: добавлен **Socket.IO**, сервер использует `http.createServer`.
- При добавлении/обновлении/удалении товара сервер рассылает события всем клиентам (`productAdded`, `productUpdated`, `productDeleted`).
- Фронтенд подключается к Socket.IO и показывает тост при получении событий в реальном времени.
- Реализована подписка на **Push-уведомления** (VAPID): кнопка «🔔 Уведомления» в шапке.
- При добавлении нового товара отправляется push всем подписанным пользователям.
- Эндпоинты: `GET /api/push/vapid-public-key`, `POST /api/push/subscribe`, `POST /api/push/unsubscribe`.

## Практика 17 (Детализация Push)
- Кнопка «⏰ Напомнить» на карточке товара открывает форму с `datetime-local`.
- Сервер планирует push через `setTimeout` и хранит таймеры в `Map`.
- Push-уведомление содержит кнопку **«Отложить на 5 минут»**.
- Service Worker обрабатывает `notificationclick` и обращается к `POST /api/push/snooze`.

## Практика 18 (Контрольная работа №3)
- Всё вышеперечисленное реализовано в едином приложении TechStore.

## Что реализовано в проекте
- Backend на **Node.js + Express**
- Frontend на **React**
- JWT-аутентификация и refresh token
- RBAC-модель доступа
- CRUD для товаров
- CRUD-операции администратора для пользователей
- Swagger-документация для backend
- Обработка ошибок и защита маршрутов

## Структура проекта
- `backend` — серверная часть приложения
- `frontend` — клиентская часть приложения

## Запуск проекта

### Backend
1. Перейти в папку `backend`
2. Установить зависимости:

```bash
npm install
```

3. Запустить сервер:

```bash
node app.js
```

Backend будет доступен по адресу:

`http://localhost:3000`

Swagger-документация:

`http://localhost:3000/api-docs`

### Frontend
1. Перейти в папку `frontend`
2. Установить зависимости:

```bash
npm install
```

3. Запустить приложение:

```bash
npm start
```

Frontend будет доступен по адресу:

`http://localhost:3001`


## Тестовые аккаунты
- `admin@techshop.local / admin123`
- `seller@techshop.local / seller123`
- `ivan.demidov.2006@yandex.ru / 123456`

